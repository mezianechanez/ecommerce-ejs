<%- include('../partials/header', { title: 'Modifier un produit' }) %>

<h2 class="page-title">Modifier un produit</h2>

<% if (errors && errors.length > 0) { %>
    <div class="alert alert-danger">
        <ul class="mb-0">
            <% errors.forEach(e => { %>
                <li><%= e.msg %></li>
            <% }) %>
        </ul>
    </div>
<% } %>

<div class="row">
    <div class="col-md-6">
        <form action="/products/edit/<%= product.id %>?_method=PUT" method="POST" class="glass">
            <div class="mb-3">
                <label class="form-label fw-bold">Nom du produit :</label>
                <input type="text" name="name" class="form-control" value="<%= product.name %>">
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Prix du produit :</label>
                <input type="number" step="0.01" name="price" class="form-control" value="<%= product.price %>">
            </div>

            <button type="submit" class="btn btn-warning">Modifier</button>
        </form>
    </div>
</div>

<%- include('../partials/footer') %>